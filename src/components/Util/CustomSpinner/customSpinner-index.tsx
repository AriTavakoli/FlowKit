import React from 'react';
import styles from './spinner.module.scss';

const CustomSpinner = () => {
  return <div className={styles["custom-spinner"]}></div>;
};

export default CustomSpinner;
